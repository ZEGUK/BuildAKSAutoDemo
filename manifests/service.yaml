apiVersion: v1
kind: Service
metadata:
  name: petstore
  namespace: namespace-workflow-1717566974395
  labels:
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app: petstore
  ports:
    - protocol: TCP
      port: 3002
      targetPort: 3002